let tasks = ["study", "gym", "eat"];

tasks.push('sleep') // here we have added new task to list
tasks.pop() // we have removed last iteam from list

console.log(tasks[1])

console.log(tasks);        // whole list
console.log(tasks[0]);     // first item
console.log(tasks.length); // number of items